# Supabase (client-side, safe to expose — the anon key is publishable)
VITE_SUPABASE_URL=https://ocacswcwxmmybztizfgw.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# NOTE: The Postgres connection string / DB password is server-side only.
# Never put it in client code or commit it to the repo.
